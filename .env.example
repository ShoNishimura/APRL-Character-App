# OpenAI API key. Never commit the real value.
OPENAI_API_KEY=

# Cost-first default. Swap the model without changing Character data or study design.
OPENAI_MODEL=gpt-5.6-luna

# Hard app-side cap. Cache hits do not count.
MAX_DAILY_LIVE_CALLS=30

PORT=8787
